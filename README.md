# go-cli-tool

A command-line tool in Go with an interactive TUI — clean command structure and flag parsing.

## Features
- Subcommand-based CLI with structured flag parsing
- Interactive terminal UI
- Idiomatic Go project layout

## Run locally
```bash
go run .
```
